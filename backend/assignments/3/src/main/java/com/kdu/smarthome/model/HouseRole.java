package com.kdu.smarthome.model;

/**
 * Enum representing different roles that can be assigned to users in a smart home system.
 */
public enum HouseRole {
    /**
     * Administrator role with additional privileges.
     */
    ADMIN,

    /**
     * Basic user role with standard privileges.
     */
    BASIC
}
